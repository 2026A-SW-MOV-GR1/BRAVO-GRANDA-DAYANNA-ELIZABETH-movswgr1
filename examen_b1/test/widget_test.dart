// Smoke test de widget — verifica que la app arranca sin excepciones.
// Las pruebas de lógica de persistencia están en persistencia_test.dart.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder – ver persistencia_test.dart para las pruebas reales', () {
    expect(true, isTrue);
  });
}
